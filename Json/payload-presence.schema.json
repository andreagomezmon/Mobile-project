{
  "$id": "https://example.com/schemas/payload-presence.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Presence Payload",
  "type": "object",
  "required": ["status"],
  "additionalProperties": false,
  "properties": {
    "status": {
      "type": "string",
      "enum": ["ONLINE", "OFFLINE"]
    }
  }
}
